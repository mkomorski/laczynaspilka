{
  "name": "laczynaspilka-checker",
  "version": "1.0.0",
  "description": "Lightweight checker for available sectors on bilety.laczynaspilka.pl",
  "scripts": {
    "start": "node index.js",
    "start:bg": "nohup node index.js >> out.log 2>&1 & echo $! > .pid && echo 'Uruchomiono w tle (PID w .pid). Zatrzymanie: kill $(cat .pid)'",
    "start:no-sleep": "caffeinate -i node index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
